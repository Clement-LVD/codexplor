---
title: "Helper functions to create and filter igraph objects"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Helper functions to create and filter igraph objects}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

# Managing igraph object
`codexplor` offering functions to manage `igraph` object. turn an edgelist into an igraph object, and filter it across the way. 

Once you have a network such as a network of internal dependancies within a project (see `get_text_network_from_project`)

- Turn your Citations Network into a directed `igraph`  network with ![wip](https://img.shields.io/badge/{igraph}-bold?style=flat&logoColor=black&logoSize=2&label=get_igraph_from_df()&labelColor=green&color=black)

        netig <- codexplor::get_igraph_from_df(net) 
        
- Once you have an igraph object, zoom on a precise function with 
![wip](https://img.shields.io/badge/{igraph}-bold?style=flat&logoColor=black&logoSize=2&label=filter_igraph_egonetwork()&labelColor=green&color=black)



